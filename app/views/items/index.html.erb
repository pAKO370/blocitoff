
<h1>Items</h1>

<div class= "items">
  <%= render partial: 'items', locals: {user: current_user,items: @items} %>
  <br>


  <%= link_to "New Item", new_user_item_path(current_user), class: 'btn btn-primary'%>
</div>
